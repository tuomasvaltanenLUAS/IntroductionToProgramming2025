from functions import *

# call a function that returns a values
# since this functions returns a value, we also
# need a variable that receives the result from the function
year = get_year()
print(year)